# Copy to apps/web/.env.production.local — paste into Vercel Production env. Do not commit.

# Neon peaceclock / main branch (direct URL for migrations; pooler OK for runtime)
DATABASE_URL=postgresql://neondb_owner:PASSWORD@ep-lingering-thunder-ajzl51i7-pooler.c-3.us-east-2.aws.neon.tech/neondb?sslmode=require

ANTHROPIC_API_KEY=sk-ant-...
VOYAGE_API_KEY=pa-...

CRON_SECRET=generate-openssl-rand-hex-32
AUDIT_SECRET=generate-openssl-rand-hex-32

# Set after first Vercel deploy URL is known, then redeploy
NEXT_PUBLIC_SITE_URL=https://peaceclock.vercel.app

# Optional MapTiler; omit for OSM fallback
# NEXT_PUBLIC_MAP_STYLE_URL=